interface:
  display_name: "Flows"
  short_description: "Author markdown agent flows"
  default_prompt: "Use $flows to write or debug a markdown agent flow with explicit inputs, prompt and code blocks, per-agent goal cards, programmatic validation loops, exhaustion routes, and visualization."
